<h2>Editar Perfil <% resource_name.to_s.humanize %></h2>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="field mb-3">
    <% f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control", placeholder:"Email" %>
  </div>
  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>
  <div class="field mb-3">
    <% f.label :password %> <i>(deixe em branco se não quiser mudar a senha)</i><br />
    <%= f.password_field :password, autocomplete: "new-password", class:"form-control", placeholder:"Nova senha" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> caracteres no mínimo</em>
    <% end %>
  </div>
  <div class="field mb-3">
    <% f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control", placeholder:"Digite novamente a senha nova" %>
  </div>
  <div class="field mb-3">
    <% f.label :current_password %>
    <%= f.password_field :current_password, autocomplete: "current-password", class:"form-control", placeholder:"Senha atual" %>
  </div>
  <div class="actions">
    <%= f.submit "Atualizar", class:"btn btn-dark" %>
  </div>
<% end %>
<br/>
<hr>
<div class="card text-center">
  <div class="card-header"><h4>Cancelar minha conta</h4></div>
    <div class="card-body">     
      <br/>
      <h6>Tem certeza que deseja cancelar sua conta?</h6>
      <br/>
      <%= button_to "Cancelar minha conta", registration_path(resource_name), 
                    data: { confirm: "Você tem certeza que deseja cancelar?" },
                    method: :delete, class:"btn btn-danger" %></p>
      <%= link_to "Voltar", :back %>
</div>
</div>
